<h1> Card Texts </h1>
  <div>
<!--
    <div class="digg_pagination">
      <%#= will_paginate @cardtexts, :container => false %>
    </div>
 -->
    <p> <%= link_to 'New Card Text', new_cardtext_path %> </p>

      <% if @cardtexts.present? %>
        <table id="dtBasicExample" class="table table-striped table-bordered table-sm">
          <thead class="thead-light" align="center">
            <tr>
              <th> N rec</th>
              <th> Description </th>
              <th> Text </th>
              <th> Holiday </th>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <%= render @cardtexts %>
        </table>
      <% else %>
        <p>
          <%= 'Empty list' %>
        </p>
      <% end %>
    <br>
  </div>

