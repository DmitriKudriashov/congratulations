<div>
  <h1> Types </h1>
  <div class="digg_pagination">
    <%= will_paginate @types, :container => false %>
  </div>

  <p> <%= link_to 'New Type', new_type_path %> </p>

    <% if @types.present? %>
 <table class="table table-bordered">
     <thead class="thead-light" align="center">
          <tr>
            <th> N </th>
            <th> Type name </th>
            <th> Holidays</th>
          </tr>
        </thead>
        <%= render @types %>
      </table>
    <% else %>
      <p>
        <%= 'Empty list' %>
      </p>
    <% end %>
  <div class="digg_pagination">
    <%= will_paginate @types, :container => false %>
  </div>
  <br>
</div>

