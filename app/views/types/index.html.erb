  <div>
    <h1> Types </h1>

    <p> <%= link_to 'New Type', new_type_path %> </p>

      <% if @types.present? %>
        <table>
          <thead class="thead-light" align="center">
            <tr>
              <th> N record</th>
              <th> Type name </th>
            </tr>
          </thead>
          <%= render @types %>
        </table>
      <% else %>
        <p>
          <%= 'Empty list' %>
        </p>
      <% end %>
    <br>
  </div>

