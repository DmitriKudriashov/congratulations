  <div>
    <h1> Persons </h1>

    <p> <%= link_to 'New Person', new_person_path %> </p>

      <% if @people.present? %>
        <table>
          <thead>
            <tr>
              <th> N record</th>
              <th> Name </th>
              <th> Email </th>
            </tr>
          </thead>
          <%= render @people %>
        </table>
      <% else %>
        <p>
          <%= 'Empty list' %>
        </p>
      <% end %>
    <br>
  </div>

